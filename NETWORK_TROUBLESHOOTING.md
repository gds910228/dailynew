# GitHub连接问题解决方案

## 问题描述
```
fatal: unable to access 'https://github.com/...': Failed to connect to github.com port 443
```

这是网络连接问题，无法直接访问GitHub。

---

## ⚡ 快速解决方案（推荐）

### 方案1：使用GitHub API上传（最简单）⭐

**优点：** 不依赖git push，直接通过API上传，绕过网络限制

**步骤：**
```batch
upload-images-api.bat
```

上传成功后，运行：
```batch
generate-urls-v2.bat
```
选择 `n` 跳过git上传（因为已通过API上传）

---

### 方案2：手动上传到GitHub

**优点：** 100%可靠，可视化操作

**步骤：**
1. 访问：https://github.com/gds910228/dailynew/tree/main/assets/images
2. 点击右上角的 "Add file" → "Upload files"
3. 拖拽图片文件上传
4. 填写commit信息（例如："Add new images"）
5. 点击 "Commit changes"

完成后运行：
```batch
generate-urls-v2.bat
```
选择 `n` 跳过上传

---

### 方案3：配置代理（如果你有VPN/代理）

**步骤：**

1. **设置代理：**
   ```batch
   setup_git_proxy.bat
   ```
   选择 `1`，输入代理地址（例如：127.0.0.1:7890）

2. **上传图片：**
   ```batch
   一键发布.bat
   ```

3. **取消代理（可选）：**
   ```batch
   setup_git_proxy.bat
   ```
   选择 `2`

**常见代理端口：**
- Clash: 7890
- V2Ray: 10809
- 其他代理软件查看设置

---

### 方案4：使用SSH代替HTTPS

**优点：** 更稳定，不需要每次输入密码

**步骤：**

1. **生成SSH密钥：**
   ```bash
   ssh-keygen -t rsa -C "your_email@example.com"
   ```
   （一直按回车，使用默认设置）

2. **复制公钥：**
   - 打开 `C:\Users\你的用户名\.ssh\id_rsa.pub`
   - 复制全部内容

3. **添加到GitHub：**
   - 访问：https://github.com/settings/keys
   - 点击 "New SSH key"
   - 粘贴公钥，点击 "Add SSH key"

4. **修改git远程地址：**
   ```bash
   git remote set-url origin git@github.com:gds910228/dailynew.git
   ```

5. **重试上传：**
   ```batch
   一键发布.bat
   ```

---

## 🔍 诊断工具

### 运行诊断脚本
```batch
python diagnose_network.py
```

这会检查：
- ✅ 网络连接状态
- ✅ Git配置
- ✅ 代理设置
- ✅ Ping测试

---

## 📝 推荐流程（按优先级）

### 1. 最简单：GitHub API上传
```batch
upload-images-api.bat
```
然后：
```batch
generate-urls-v2.bat  (选择n)
```

### 2. 最可靠：手动上传
通过GitHub网页界面上传图片

### 3. 使用代理
如果你有VPN/代理，配置git代理

---

## ⚠️ 常见问题

### Q1: upload-images-api.bat 失败？
**A:** 检查 `admin/config.local.js` 中的GitHub Token是否有效

### Q2: 手动上传找不到目录？
**A:** 确保仓库结构正确，需要先创建 `assets/images/` 目录

### Q3: 配置代理后还是失败？
**A:**
1. 确认代理软件正在运行
2. 检查代理地址和端口是否正确
3. 尝试在浏览器中访问GitHub确认代理有效

### Q4: SSH方式无法连接？
**A:**
1. 确认SSH公钥已正确添加到GitHub
2. 测试连接：`ssh -T git@github.com`
3. 检查防火墙是否允许SSH连接（端口22）

---

## 🎯 完整示例

### 使用API上传的完整流程：

```batch
# 1. 上传图片（通过API，不需要git push）
upload-images-api.bat

# 2. 生成URL并打开后台管理
generate-urls-v2.bat
# 选择 n (跳过上传，因为已通过API上传)
# 选择 y (打开后台管理)

# 3. 在后台管理页面
# - URL已自动填充
# - 填写标题和简述
# - 点击提交
```

---

## 📞 需要帮助？

如果以上方案都无法解决，请提供：
1. 运行 `python diagnose_network.py` 的输出
2. 你使用的网络环境（家庭/公司/学校）
3. 是否使用VPN/代理
4. 具体的错误信息

---

## ✅ 快速参考

| 方案 | 难度 | 可靠性 | 速度 |
|------|------|--------|------|
| GitHub API | ⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| 手动上传 | ⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |
| 配置代理 | ⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ |
| SSH方式 | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ |

**推荐：先试方案1（GitHub API），最简单快速！**
